export type AddAccountModel = {
  name: string;
  lastName: string;
  email: string;
  contactNumber: string;
  birthDate: Date;
  maritalStatus?: string;
  genre?: string;
  currentState: string;
  city?: string;
  cpf: string;
  profession?: string;
};
