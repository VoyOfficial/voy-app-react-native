type SearchPlaceModel = {
  id: number;
  rating: string;
  isSaved: boolean;
  amountOfReviews: string;
  imageUri: string;
  location: string;
  title: string;
  myDistanceOfLocal: string;
};

export default SearchPlaceModel;
